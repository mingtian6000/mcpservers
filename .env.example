# Jenkins server connection
JENKINS_URL=http://your-jenkins.example.com:8080
JENKINS_USER=your-username
JENKINS_PASSWORD=your-api-token-or-password
# Optional: custom CA bundle for TLS
# JENKINS_CA_BUNDLE=/path/to/ca-bundle.crt
